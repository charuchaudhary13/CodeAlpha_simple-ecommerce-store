<!DOCTYPE html>
<html>
<head>
  <title>Products</title>
</head>
<body>
  <h1>All Products</h1>
  <ul>
    <% products.forEach(product => { %>
      <li>
        <h2><%= product.name %></h2>
        <p>₹<%= product.price %></p>
        <p><%= product.description %></p>
        <% if (product.imageUrl) { %>
          <img src="<%= product.imageUrl %>" alt="Product Image" width="150">
        <% } %>
      </li>
    <% }) %>
  </ul>
</body>
</html>
